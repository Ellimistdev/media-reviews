<h1><%= "#{@user.attributes['username']}" %></h1>

<% if current_user == @user %>
  <%= render 'form', url: user_path(@user), user: @user %>
<% end %>
<ul>
  <li><%= "User id: #{@user.attributes['id']}" %></li>
  <li><%= link_to "User Reviews", user_reviews_path(@user) %></li>
  <li><%= link_to "User Views", user_views_path(@user) %></li>
</ul>
